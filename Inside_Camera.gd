extends Camera2D

# I want this script to zoom in whenever the player enters.
# This is to make sure they can't see past the walls inside and make it look smaller

# Declare member variables here. Examples:
# var a = 2
# var b = "text"
# Get location variable from 
#onready var location = get_node()

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
